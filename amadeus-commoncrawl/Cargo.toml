[package]
name = "amadeus-commoncrawl"
version = "0.1.0"
license = "Apache-2.0"
description = "Derive Macro TODO"
homepage = "https://github.com/alecmocatta/amadeus"
repository = "https://github.com/alecmocatta/amadeus"
authors = ["..."]
keywords = [ "arrow", "parquet", "hadoop" ]
readme = "README.md"
edition = "2018"

[dependencies]
amadeus-core = { version = "0.1", path = "../amadeus-core" }
amadeus-derive = { version = "0.1", path = "../amadeus-derive" }
flate2 = "1.0"
reqwest = "0.9"
reqwest_resume = "0.2"
warc_parser = { git = "https://github.com/alecmocatta/warc_nom_parser" }
serde_closure = { version = "0.1" }
serde = { version = "1.0", features = ["derive"] }
